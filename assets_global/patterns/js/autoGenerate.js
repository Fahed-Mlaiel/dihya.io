// autoGenerate.js – Génération automatique de modules/templates
module.exports = function autoGenerate(template, config) {
  // Exemple métier : génération automatique
  return `Auto-généré ${template} avec config: ${JSON.stringify(config)}`;
};
