// seoService.js – Module SEO automatique
module.exports = {
  generateMetaTags: (info) => {
    // Exemple métier : génération de balises meta
    return `<meta name="description" content="${info.description}">`;
  }
};
