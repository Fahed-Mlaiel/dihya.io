// backendApi.js – Blueprint génération d’API backend (Node.js, Flask)
module.exports = function generateBackendApi(config) {
  // Exemple métier : génération d’une API Node.js/Flask
  return `Backend API générée avec config: ${JSON.stringify(config)}`;
};
