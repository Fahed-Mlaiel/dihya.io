// plugin.js – Blueprint génération de plugins (analytics, CMS, Stripe, etc.)
module.exports = function generatePlugin(config) {
  // Exemple métier : génération d’un plugin
  return `Plugin généré avec config: ${JSON.stringify(config)}`;
};
