class ExampleAnalyticsPlugin {
  constructor(options) {
    this.options = options;
  }
  init() {
    // Initialisation du plugin
  }
  track(event, data) {
    // Logique de tracking
  }
}

export default ExampleAnalyticsPlugin;
