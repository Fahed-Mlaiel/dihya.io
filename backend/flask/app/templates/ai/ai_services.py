"""
AI Services – Fallback universel, multilingue, RGPD, sécurité, plugins-ready
"""

def ai_fallback(project_id, lang='fr'):
    """Fallback IA/AI universel (Ollama, Mixtral, LLaMA, multilingue, RGPD)."""
    return {'id': project_id, 'name': 'Fallback', 'lang': lang, 'result': None}
