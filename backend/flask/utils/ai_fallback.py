"""
Fallback IA open source (LLaMA, Mixtral, Mistral)
"""
def generate_with_fallback(prompt: str) -> str:
    # Appel IA open source (exemple, à adapter)
    # Fallback LLaMA/Mixtral/Mistral
    return f"[IA Fallback] Réponse générée pour: {prompt}"
