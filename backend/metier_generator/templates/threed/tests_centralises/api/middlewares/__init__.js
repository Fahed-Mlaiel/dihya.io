// Exporte tous les tests middlewares API threed
// Utilisation : require('./api/middlewares') pour charger tous les tests middlewares
module.exports = {
  testApiMiddlewares: require('./test_api_middlewares'),
  // Ajoutez ici d’autres tests dès qu’ils existent
};
