const { checkAccessibility } = require('./accessibility');
// Exporte les helpers, runners et hooks d’accessibilité
module.exports = {
  checkAccessibility,
  // Ajouter ici d’autres helpers/runners/hooks si besoin
};
