/* global console */
// sample_core.js
// Exemple d'utilisation des core views (JS)
const core = require('../core');
console.log(core.renderCoreView ? core.renderCoreView('Test') : 'Core view sample');
