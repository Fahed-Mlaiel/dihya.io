// Test avancé pour middlewares.js du module api/samples/middlewares
// Remplacer l'import par le chemin réel si besoin
// const middlewares = require('../../../../../api/samples/middlewares/middlewares');
describe('API Samples Middlewares', () => {
  it('should expose expected API', () => {
    // expect(middlewares).toBeDefined();
    expect(true).toBe(true);
  });
});
