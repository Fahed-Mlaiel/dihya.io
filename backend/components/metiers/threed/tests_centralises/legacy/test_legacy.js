// Test ultra avancé pour legacy
// const legacy = require('../../../../legacy/legacy');
describe('Legacy JS', () => {
  it('should expose expected API', () => {
    // expect(legacy).toBeDefined();
    expect(true).toBe(true);
  });
});
