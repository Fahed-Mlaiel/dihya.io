// legacy_mock.js – Mock ultra avancé pour fallback legacy (JS)
const legacyMock = {
  id: 'legacy-mock-001',
  type: 'fallback',
  status: 'active',
  description: 'Mock legacy pour tests de compatibilité, edge cases, audit.'
};

module.exports = legacyMock;
