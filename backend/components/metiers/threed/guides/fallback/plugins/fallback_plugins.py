# guides/fallback/fallback_plugins.py – Fallbacks et tests avancés pour guides plugins

def fallback_plugin_config():
    # Fallback de configuration plugin pour tests d’intégration
    return {
        'name': 'plugin-fallback',
        'version': '0.0.1',
        'description': 'Fallback de test pour plugin',
    }
