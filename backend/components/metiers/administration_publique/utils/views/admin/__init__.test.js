// __init__.test.js – Test d'initialisation avancée admin views threed
const admin = require('./__init__');
describe('Init JS admin views threed', () => {
  it('doit exposer le module admin', () => {
    expect(admin).toBeDefined();
  });
});
