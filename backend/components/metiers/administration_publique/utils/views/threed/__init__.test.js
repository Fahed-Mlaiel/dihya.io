// __init__.test.js – Test d'initialisation threed views JS
const threed = require('./__init__');
describe('Init JS threed views', () => {
  it('expose le module threed', () => {
    expect(threed).toBeDefined();
  });
});
