// __init__.test.js – Test d'initialisation avancée conformity views threed
const conformity = require('./__init__');
describe('Init JS conformity views threed', () => {
  it('doit exposer le module conformity', () => {
    expect(conformity).toBeDefined();
  });
});
