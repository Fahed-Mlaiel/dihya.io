// security.test.js – Test d’intégration avancé Security
const security = require('./test_security');
test('Security module est importable et fonctionnel', () => {
  expect(security).toBeDefined();
});
