// index.js – Point d'entrée ultra avancé pour les utilitaires (clé en main)
const core = require('./core');
const i18n = require('./i18n');
const rbac = require('./rbac');

module.exports = {
  core,
  i18n,
  rbac
};
