// fixtures.test.js – Test d’intégration avancé Fixtures
const fixtures = require('./test_fixtures');
test('Fixtures module est importable et fonctionnel', () => {
  expect(fixtures).toBeDefined();
});
