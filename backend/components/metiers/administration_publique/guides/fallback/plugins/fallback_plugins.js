// guides/fallback/fallback_plugins.js – Fallbacks et tests avancés pour guides plugins

function fallbackPluginConfig() {
  // Fallback de configuration plugin pour tests d’intégration
  return {
    name: 'plugin-fallback',
    version: '0.0.1',
    description: 'Fallback de test pour plugin',
  };
}

module.exports = { fallbackPluginConfig };
