// validator_sample.js – Exemple de validation ultra avancé
/**
 * Simule une validation legacy Ressources_humaines
 */
function validatorSample(input) {
  // ... logique de validation avancée ...
  return typeof input === 'string' && input.length > 3;
}

module.exports = validatorSample;
