// hotellerie_views.js
// Helpers et endpoints spécifiques au rendu hotellerie (Node.js)
function renderhotellerie(model) {
  return `Rendu hotellerie du modèle: ${model}`;
}
module.exports = { renderhotellerie };
