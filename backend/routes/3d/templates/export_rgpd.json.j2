{
  "project": "{{ project.name }}",
  "owner": "{{ project.owner }}",
  "assets": [
    {% for asset in project.assets %}
    {
      "name": "{{ asset.name }}",
      "type": "{{ asset.type }}"
    }{% if not loop.last %},{% endif %}
    {% endfor %}
  ]
}
